apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: clientservice-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - host: arch.homework
    http:
      paths:
      - path: /otusapp($|/)(.*)
        backend:
          serviceName: clientservice-service
          servicePort: 8000
