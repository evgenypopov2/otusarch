apiVersion: v1
kind: Service
metadata:
  name: {{ include "clientservice-chart.fullname" . }}
  labels:
    {{- include "clientservice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: web
  selector:
    {{- include "clientservice-chart.selectorLabels" . | nindent 4 }}
